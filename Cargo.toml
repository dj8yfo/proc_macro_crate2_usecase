[workspace]
members = ["client_lib"]

[workspace.package]
# shared version of all public crates in the workspace
version = "0.1.0"

[workspace.dependencies]
foo = { package = "borsh", version = "1.1.2", features = ["derive"] }
